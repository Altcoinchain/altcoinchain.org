import styled from 'styled-components';
import { PageContainerStyled } from '@/common/styles/page-container.styled';

export const HomeStyled = styled(PageContainerStyled)`
    svg {
      max-width: 24rem;
        padding: 1rem;
      transition: fill 250ms ease-in-out;
    }
`;
