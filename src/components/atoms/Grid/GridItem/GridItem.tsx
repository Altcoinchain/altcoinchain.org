import { StyledGridItem } from './GridItem.styled';
import { StyledGridItemProps } from './GridItem.types';

const GridItem = (props: StyledGridItemProps) => <StyledGridItem {...props} />;

export default GridItem;
