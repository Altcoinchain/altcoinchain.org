import { MessageDescriptor } from 'react-intl';

const homeDescriptors: Record<string, MessageDescriptor> = {
    title: {
        id: 'home.title',
        defaultMessage: 'POW Layer 1 Blockchain',
    },
    description: {
        id: 'home.description',
        defaultMessage: 'Fair, Permissionless , no premine!\n\nEthash POW, 2 ALT blockreward.',
    },
};

export default homeDescriptors;
