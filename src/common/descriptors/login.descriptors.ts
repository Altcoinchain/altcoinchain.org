import { MessageDescriptor } from 'react-intl';

const loginDescriptors: Record<string, MessageDescriptor> = {
    title: {
        id: 'login.title',
        defaultMessage: 'Sign in to your account',
    },
};

export default loginDescriptors;
